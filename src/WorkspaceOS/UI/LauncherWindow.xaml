<Window x:Class="WorkspaceOS.UI.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Launcher" Width="560" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Topmost="True"
        WindowStartupLocation="CenterScreen" Deactivated="Window_Deactivated">
    <Border Background="#F2101010" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="8" Padding="10">
        <StackPanel>
            <TextBox x:Name="QueryBox" Style="{StaticResource DarkTextBox}" FontSize="18" BorderThickness="0"
                     Background="Transparent" TextChanged="QueryBox_TextChanged" PreviewKeyDown="QueryBox_KeyDown"/>
            <ListBox x:Name="Results" Style="{StaticResource DarkList}" MaxHeight="420" Margin="0,8,0,0"
                     MouseDoubleClick="Results_DoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4,3">
                            <TextBlock Text="{Binding Title}" FontSize="14" Foreground="#E0E0E0" FontFamily="Consolas"/>
                            <TextBlock Text="{Binding Subtitle}" FontSize="11" Foreground="#8A8A8A" FontFamily="Consolas"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</Window>
